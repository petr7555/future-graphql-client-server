{
  "name": "server",
  "version": "1.0.0",
  "description": "h3. How to run\r Run `node server.js` in this folder, then go to `http://localhost:4000/graphql`.",
  "main": "server.js",
  "dependencies": {
    "apollo-datasource-rest": "^0.9.5",
    "apollo-server": "^2.19.0"
  },
  "devDependencies": {},
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "set REST_URL=http://localhost:5000/&& node server.js"
  },
  "author": "",
  "license": "ISC"
}
